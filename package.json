{
  "name": "playwright-testing-example",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "npx playwright test && npm run test:component",
    "test:unit": "npx playwright test --project=unit",
    "test:integration": "npx playwright test --project=integration",
    "test:e2e": "npx playwright test --project=e2e",
    "test:component": "cd frontend && npx playwright test -c playwright-ct.config.ts",
    "install:all": "npm install && cd server && npm install && cd ../frontend && npm install",
    "install:browsers": "npx playwright install chromium && cd frontend && npx playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
